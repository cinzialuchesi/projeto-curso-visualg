/*
Var
  numero, fatorial, contador: inteiro

Inicio

  escreva ("Digite o numero para calcular a fatorial: ")
  leia (numero)
  
  fatorial :=1
  
  para contador de 1 ate numero faca
      fatorial := fatorial * contador
  fimpara
  
  escreva ("Resultado fatorial de ", numero, " é: ", fatorial)
  
*/

function contador1(numero1) {{
    for (contador = 1; contador <= numero; contador++) 
        return fatorial= fatorial * contador;
        } 
        const resultado=fatorial 
    }   

function acaoBotao(){

    const numero = prompt(" Digite o numero para calcular a fatorial:  ");
    const fatorial = 1;
    const contador = 1; 
    
    const resultadoAserImpresso = contador1(numero1)
        
    document.querySelector("p").innerText=("Resultado fatorial de " + numero + " é: " + resultadoAserImpresso);
} 
        
   

    
         
   
    
         

